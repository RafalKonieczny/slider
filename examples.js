let myFirstFunction = (name, greeting) => {
    console.log(greeting + "! Mam na imię " + name + "!");
};

myFirstFunction("Rafał", "Hej");
myFirstFunction("Jan", "Dzień dobry");


let multiplyBy2 = (number) => {
    console.log(number*2);
}

multiplyBy2(2);

let multiply = (a, b) => {
    console.log(a * b);
}

multiply(2, 4);

let square = (number) => {
    console.log(number * number);
}

square(2);